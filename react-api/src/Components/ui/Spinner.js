import React from 'react'
import spinner from '../../img/spinner.gif'

const Spinner = () => {
  return (
    <img src={spinner} style={{textAlign:"center",margin:"auto",display:"block"}} alt=',Loading..'></img>
  )
}

export default Spinner